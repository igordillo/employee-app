import { environment } from 'src/environments/environment';

export const EMPLOYEES_API_ENDPOINT = environment.API_REST_URL + 'employees';

export const WORK_POSITIONS_API_ENDPOINT = 'https://www.ibillboard.com/api/positions';

export const FORMAT_DATE = 'dd-MM-YYYY';

export const DEFAULT_LANG = 'en';
